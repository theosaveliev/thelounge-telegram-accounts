# thelounge-telegram-accounts

Create TheLounge accounts with Telegram userbot

This is a self-hosted tool built for a local community setup. It assumes:

- A running TheLounge container named `thelounge`
- A Docker socket available at `/var/run/docker.sock`
- Telegram @usernames are used as TheLounge logins
